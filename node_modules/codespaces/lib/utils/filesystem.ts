import { readFile } from "fs";

export const readFileAsync = (path: string, encoding: BufferEncoding) => {
  return new Promise((resolve, reject) => {
    readFile(path, encoding, (error, data) =>
      error ? reject(error) : resolve(data)
    );
  });
};
